# 🤖 لوحة تحكم ذراع الروبوت

<div align="center">

![Version](https://img.shields.io/badge/الإصدار-2.0-blue.svg)
![PHP](https://img.shields.io/badge/PHP-7.4+-purple.svg)
![MySQL](https://img.shields.io/badge/MySQL-5.7+-orange.svg)

**واجهة ويب عصرية للتحكم في 4 محركات سيرفو مع قاعدة بيانات MySQL**

![](c.jpg)

</div>

---

## 📋 المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [المميزات](#-المميزات)
- [التثبيت](#-التثبيت)
- [كيف يعمل؟](#-كيف-يعمل)
- [نقطة الوصول API](#-نقطة-الوصول-api)
- [المشاكل والحلول](#-المشاكل-والحلول)

---

## 🎯 نظرة عامة

مشروع لوحة تحكم ويب لإدارة ذراع روبوت بـ 4 محركات سيرفو. يوفر واجهة تفاعلية لحفظ وضعيات الذراع في قاعدة بيانات MySQL، مع إمكانية قراءتها من متحكم ESP32/ESP8266.

### ماذا يفعل؟

1. **التحكم**: ضبط زوايا 4 محركات (0-180°) عبر سلايدرات تفاعلية
2. **الحفظ**: تخزين الوضعيات في قاعدة البيانات
3. **العرض**: استعراض وحذف الوضعيات المحفوظة
4. **المزامنة**: قراءة أحدث وضعية عبر ESP

---

## ✨ المميزات

### 🎨 الواجهة
- ✅ تصميم عصري بتدرجات لونية متحركة
- ✅ تأثيرات Glassmorphism احترافية
- ✅ حركات سلسة على جميع العناصر
- ✅ متجاوب مع الجوال والتابلت

### ⚙️ الوظائف
- 🎚️ **4 سلايدرات تفاعلية** مع عرض القيمة لحظياً
- 💾 **حفظ الوضعيات** في MySQL
- 📊 **جدول السجلات** مع إمكانية الحذف
- 🔄 **إعادة تعيين سريعة** لجميع المحركات إلى 90°
- 📡 **API ديناميكي** لقراءة البيانات من ESP


<div align="center">

[![عرض حي للوحة تحكم السيرفو](https://img.youtube.com/vi/Ws5od4z1TfI/hqdefault.jpg)](https://youtu.be/Ws5od4z1TfI)

</div>


---

## 📦 التثبيت

### المتطلبات
```
✅ XAMPP (Apache + MySQL + PHP)
✅ متصفح ويب
✅ محرر نصوص
```

### الخطوات

**1. إنشاء قاعدة البيانات**

افتح phpMyAdmin وأنشئ قاعدة البيانات:

```sql
CREATE DATABASE robot_control;
USE robot_control;

CREATE TABLE angles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    servo1 INT NOT NULL,
    servo2 INT NOT NULL,
    servo3 INT NOT NULL,
    servo4 INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**2. ضبط الاتصال**

عدّل في ملف `index.php`:

```php
$password = "كلمة_المرور_هنا"; // ⚠️ مهم: ضع كلمة مرورك
```

**3. رفع الملفات**

انسخ المشروع إلى:
```
C:\xampp\htdocs\robot-arm\
```

**4. التشغيل**

افتح المتصفح على:
```
http://localhost/robot-arm/index.php
```

---

## 🏗️ كيف يعمل؟

### المعمارية

```
┌──────────┐         ┌──────────┐         ┌──────────┐
│  المتصفح  │◄───────►│   PHP    │◄───────►│  MySQL   │
│  (الواجهة)│   HTTP  │ (Backend)│   SQL   │  (قاعدة) │
└──────────┘         └──────────┘         └──────────┘
                           ▲
                           │ HTTP GET
                           │
                    ┌──────┴──────┐
                    │   ESP32     │
                    │  (المتحكم)  │
                    └─────────────┘
```

### سير العمل

1. **المستخدم** يحرك السلايدرات ويضغط "حفظ"
2. **PHP** يرسل البيانات إلى MySQL
3. **ESP** يطلب أحدث وضعية من `angles.php`
4. **المحركات** تتحرك حسب الزوايا المستلمة

---

## 📡 نقطة الوصول API

### ملف `angles.php`

**الطلب**: `GET http://server-ip/angles.php`

**الاستجابة**:
```
90,120,45,180
```

### مثال ESP32

```cpp
#include <WiFi.h>
#include <HTTPClient.h>

void getAngles() {
    HTTPClient http;
    http.begin("http://192.168.1.100/angles.php");
    
    if (http.GET() == 200) {
        String data = http.getString();
        // البيانات: "90,120,45,180"
        // حرك المحركات حسب الزوايا
    }
    http.end();
}
```

---

## 💡 لماذا هذه الطريقة؟

### 🆚 مقارنة مع طريقة الملفات

| الميزة | طريقتنا (قاعدة البيانات) | طريقة الملفات |
|--------|--------------------------|----------------|
| **مصدر البيانات** | ✅ واحد فقط | ❌ اثنان (DB + ملف) |
| **التحديث التلقائي** | ✅ فوري | ❌ يدوي |
| **الأمان** | ✅ عالي | ❌ مشاكل الصلاحيات |
| **القراءة المتزامنة** | ✅ آمنة | ❌ قد تتعطل |
| **سهولة الاستخدام** | ✅ نقرة واحدة | ❌ نقرتان |

### الفوائد

1. **مصدر واحد للحقيقة** 🎯 - كل البيانات في مكان واحد
2. **بيانات لحظية** ⚡ - ESP يحصل على أحدث وضعية دائماً
3. **أمان أعلى** 🔒 - لا حاجة لصلاحيات كتابة الملفات
4. **استقرار** 🛡️ - قاعدة البيانات تتعامل مع العمليات المتزامنة
5. **تجربة أبسط** 🎯 - زر حفظ واحد فقط

---

## 🚧 المشاكل والحلول

### 1️⃣ خطأ Access Denied

**المشكلة**:
```
Fatal error: Access denied for user 'root'@'localhost'
```

**السبب**: كلمة المرور فارغة في الكود بينما MySQL يطلب كلمة مرور

**الحل**:
```php
$password = "كلمة_مرورك_الصحيحة";
```

---

### 2️⃣ عرض الوضعيات المحفوظة

**المشكلة**: الجدول غير موجود في الكود الأولي

**الحل**: إضافة استعلام وعرض البيانات:
```php
$result = $conn->query("SELECT * FROM angles ORDER BY id DESC");
while($row = $result->fetch_assoc()) {
    // عرض البيانات في الجدول
}
```

---

### 3️⃣ التصميم

**المشكلة**: واجهة بسيطة بدون تنسيق

**الحل**:
- إنشاء ملف CSS شامل
- استخدام CSS Grid للسلايدرات
- إضافة تأثيرات حركية
- تصميم عصري بتدرجات لونية

---

### 4️⃣ وظيفة الحذف

**المشكلة**: لا توجد طريقة لحذف السجلات

**الحل**: إضافة رابط حذف آمن:
```php
if(isset($_GET['delete'])) {
    $stmt = $conn->prepare("DELETE FROM angles WHERE id = ?");
    $stmt->bind_param("i", $_GET['delete']);
    $stmt->execute();
}
```

**الحماية**:
- استخدام Prepared Statements (ضد SQL Injection)
- رسالة تأكيد JavaScript
- إعادة توجيه بعد الحذف

---

## 📖 الاستخدام

### الخطوات الأساسية

1. **اضبط الزوايا** 🎚️
   - حرك السلايدرات للزوايا المطلوبة
   
2. **احفظ الوضعية** 💾
   - اضغط "Save Position"
   
3. **ESP يقرأ البيانات** 📡
   - المتحكم يطلب البيانات من `angles.php`
   - المحركات تتحرك للوضعية الجديدة

4. **إدارة السجلات** 🗂️
   - استعرض الوضعيات المحفوظة
   - احذف ما لا تحتاجه

---

## 🔮 تطويرات مستقبلية

- [ ] تسجيل دخول متعدد المستخدمين
- [ ] اتصال WebSocket للتحديث الفوري
- [ ] تشغيل متسلسل لعدة وضعيات
- [ ] محاكي ثلاثي الأبعاد للذراع
- [ ] تصدير/استيراد الوضعيات
- [ ] وضع ليلي (Dark Mode)
- [ ] تطبيق موبايل

---
.

---


<div align="center">

**صُنع بـ ❤️ في السعودية**

⭐ إذا أعجبك المشروع، لا تنسَ النجمة!

</div>
